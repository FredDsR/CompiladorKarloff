void main () {
    newVar integer soma;
    newVar integer valor;

    soma = 0;
    
    repeat {
        valor = System.readint();
        soma = (soma + valor); 
    } until ((valor == 0));

    System.output(valor);
}