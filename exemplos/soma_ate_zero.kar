void main () {
    newVar integer soma;
    newVar integer valor;

    soma = 0;
    valor = System.readint();
    repeat {
        soma = (soma + valor); 
        valor = System.readint();
    } until ((valor == 0));

    System.output(soma);
}